# fizun-test-feature-ecs

> **Branch**: `feature-ecs` — deployed by DevOps Agent

## 🚀 Deployment Info

| Field | Value |
|-------|-------|
| **App** | nginx |
| **Target** | ECS Fargate |
| **Branch** | `feature-ecs` |
| **Region** | us-east-1 |
| **Live URL** | See ALB DNS output in Actions logs |
| **Last Deploy** | 2026-03-14 13:18 UTC |

## 🏗 Infrastructure

- **Container Registry**: Amazon ECR
- **Compute**: AWS ECS Fargate (serverless containers, no EC2)
- **Load Balancer**: Application Load Balancer (ALB)
- **State**: S3 Terraform backend (per-branch key)

## ⚙️ Pipeline

```
Terraform (ECR + ECS + ALB) → Docker Build & Push (ECR) → ECS Service Deploy → Verify → Notify
```

## 📁 Key Files

| File | Purpose |
|------|---------|
| `terraform/main.tf` | ECR, ECS Cluster, Fargate Service, ALB, IAM, Security Groups |
| `Dockerfile` | nginx container image definition |
| `.github/workflows/deploy.yml` | CI/CD pipeline: infra + image build/push + ECS deploy |
| `.github/workflows/destroy.yml` | Infrastructure teardown |

## 🐳 Container Architecture

```
Internet
   │
   ▼
ALB (port 80)
   │
   ▼
ECS Fargate Service
   │
   ▼
nginx Container (ECR Image)
```

## 🔧 How to Deploy

1. Push changes to branch `feature-ecs`
2. GitHub Actions will automatically trigger the deploy pipeline:
   - Terraform provisions ECR, ECS Cluster, Fargate Service, and ALB
   - Docker builds the nginx image and pushes to ECR
   - ECS service is updated with the new image
3. ALB DNS name is printed at the end of the Actions run

## 💣 How to Destroy

Use the DevOps Agent bot:
```
/destroy
```
Or trigger `.github/workflows/destroy.yml` manually in GitHub Actions.
This will destroy all ECS, ECR, ALB, and supporting infrastructure for this branch.

---
*Auto-generated by DevOps Agent on 2026-03-14 13:18 UTC*